/**
 * Board height
 * @type {number}
 */
export const HEIGHT =10;
/**
 * Board width
 * @type {number}
 */
export const WIDTH = 10;

/**
 * Map of ship size -> count.
 * Be careful! Don't use big ships without smaller.
 *
 * @type {{1: number, 2: number, 3: number, 4: number}}
 */
export const SHIPS_SIZE_COUNT = {
    "1": 4,
    "2": 3,
    "3": 2,
    "4": 1
};
